import { style } from "typestyle";

const ProcessMapLinkStyle = {
  commonLink: style({
    display: "flex",
    alignItems: "center",
    marginLeft: "auto",
    marginRight: 0,
    cursor: "pointer"
  })
};

export default ProcessMapLinkStyle;
