import { style } from "typestyle";

const ProcessDateStyle = {
  commonProcessDate: style({
    display: "flex",
    alignItems: "center",
    marginBottom: "12px"
  }),
  dateColor: style({
    color: "#333333"
  }),
  blockPart: style({
    display: "block",
    width: "5vw"
  })
};

export default ProcessDateStyle;
