import { style } from "typestyle";

const MenuStyles = {
  common: style({
    background: "#404064",
    boxShadow: "0px 0px 24px rgba(0, 0, 0, 0.3)",
    height: "89.2vh",
    overflowY: "hidden",
    paddingTop: "18px",
    paddingLeft: "28px",
    overflowX: "hidden"
  })
};

export default MenuStyles;
