import { style } from "typestyle";

const MenuStyles = {
  common: style({
    background: "#404064",
    boxShadow: "0px 0px 24px rgba(0, 0, 0, 0.3)",
    //width: "16.4vw",
    height: "708px",
    overflowY: "hidden",
    paddingTop: "18px",
    paddingLeft: "28px"
  })
};

export default MenuStyles;
